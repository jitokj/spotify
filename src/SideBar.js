import React from 'react';
import "./SideBar.css";

const SideBar = () => {
    return (
        <div className="sidebar">
            <h1>Sidebar</h1>
        </div>
    );
};

export default SideBar;